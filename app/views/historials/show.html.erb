

<h3> This is the owner name: <small> <%= @historial.owner_name %> </small> </h3>
<h3> This is the reason: <small> <%= @historial.consult_reason %> </small> </h3>

<%= link_to "Edit", edit_historial_path(@historial) %></td>
<%= link_to "Delete", historial_path(@historial), method: :delete, data: {confirm: "Are you sure you want to delete this historial?"} %></td>
<%= link_to "Back", historials_path %></td>